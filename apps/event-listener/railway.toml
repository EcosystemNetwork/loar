[build]
builder = "DOCKERFILE"
dockerfilePath = "../../apps/event-listener/Dockerfile"
dockerContext = "../.."

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
