[build]
builder = "DOCKERFILE"
dockerfilePath = "../../apps/server/Dockerfile"
dockerContext = "../.."

[deploy]
startCommand = "node apps/server/dist/workers/generation.worker.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
