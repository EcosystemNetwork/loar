node_modules
.turbo
.DS_Store
coverage/
.nyc_output

# Built TypeScript output for server-side packages — never commit; CI builds these.
# packages/abis/dist is intentionally committed (consumed as a published-style
# package by other apps). apps/mobile/dist is Expo bundle output kept for now.
apps/mcp-gateway/dist/
apps/mcp/dist/
apps/solana-indexer/dist/

.env
.env.local
.playwright-mcp
.claude
CLAUDE.md
firebase-service-account.json
firebase-sa-key-*.json
build.log
.gitnexus
.firebase
apps/contracts/broadcast/

# Playwright
test-results/
playwright-report/
.playwright-artifacts-*/

# Visual QA outputs
docs/qa/visual-*/
# Media generation outputs — content goes to Pinata/Lighthouse/Firestore, not git.
*-output/
ingest-log.json
repair-broken-urls-*.json
dead-gcs-urls-report-*.json
rescue-gcs-hero-clips-*.json
credit-loss-scan-*.json

# Redis dump files (accidental commits)
*.rdb
dump.rdb

# Firebase emulator debug logs
firestore-debug.log
firebase-debug.log
ui-debug.log
database-debug.log
pubsub-debug.log

# Rescued ephemeral media awaiting rehost (see scripts/backfill-ephemeral-generations.ts)
rescued-media/

# Local Netlify folder
.netlify
